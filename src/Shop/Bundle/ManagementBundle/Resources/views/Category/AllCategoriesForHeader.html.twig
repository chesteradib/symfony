{% for category in categories %}
        <a href="javascript:void(0);"
           class="show_category_trigger"
           data-id="{{category.id}}"
            >
            {{ category.Name }}
        </a>
{%endfor%}

